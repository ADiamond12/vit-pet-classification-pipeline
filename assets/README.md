# Screenshot Assets

This folder contains public-safe screenshots for the project README and portfolio references.

- `ui-empty.png`: sanitized Streamlit upload flow, with no external company branding, datasets, model weights, local user paths, or private data.

Prediction screenshots are not included yet because the public repository does not ship a trained checkpoint. Add prediction screenshots only after using a project-owned public checkpoint or a locally trained checkpoint that is safe to demonstrate.
